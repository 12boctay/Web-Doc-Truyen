# Demo CI/CD - Branch phutest

This branch was created by **phutest** to demonstrate CI/CD workflow when multiple people merge into main.

## Scenario

- Branch `phutest` — changes from developer Phu
- Branch `main` — production branch
- When both branches merge, CI/CD pipeline runs automatically
